echo ""
echo "=== 6. redis-infra 저장소 클론 ==="
REPO_URL="git@github.com:JanbBongJun/redis-infra.git"
CLONE_DIR="$HOME/redis-infra"

if [ -d "$CLONE_DIR/.git" ]; then
echo "이미 클론되어 있습니다. pull을 실행합니다."
git -C "$CLONE_DIR" pull
else
  git clone "$REPO_URL" "$CLONE_DIR"
fi
